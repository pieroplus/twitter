<div class="users-edit">
  <h2>ユーザー情報の変更<% resource_name.to_s.humanize %></h2>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
    <%= devise_error_messages! %>

    <div class="field">
      <%= f.label :email ,"メールアドレス"%><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
    </div>

    <div class="field">
      <%= f.label :name ,"名前（ニックネーム）"%><br />
      <%= f.text_field :name, autofocus: true, autocomplete: "name" %>
    </div>

    <!-- <div class="field"> -->
      <!-- <%= f.label :icon ,"アイコン"%><br /> -->
    <!-- </div> -->

    <!-- <div class="field"> -->
      <!-- <%= f.label :image ,"ヘッダー画像"%><br /> -->
    <!-- </div> -->

    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
    <% end %>

    <div class="field">
      <%= f.label :password ,"パスワード"%><br /><i>(変更をしない場合は空白)</i><br />
      <%= f.password_field :password, autofocus: true, autocomplete: "off", placeholder: "新しいパスワードを入力" %>
    </div>

    <div class="field">
      <%= f.label :password ,"パスワード"%><br /><i>(変更をしない場合は空白)</i><br />
      <%= f.password_field :confirmation_password, autofocus: true, autocomplete: "off", placeholder: "新しいパスワードを再入力" %>
    </div>

    <div class="field">
      <%= f.label :current_password ,"現在のパスワード"%><br />
      <i>(変更を確認するために現在のパスワードが必要です)</i><br />
      <%= f.password_field :current_password, autofocus: true, autocomplete: "off", placeholder: "現在のパスワードを入力"%>
    </div>

    <div class="actions">
      <%= f.submit "変更を保存" %>
    </div>
  <% end %>

  <%= link_to "ホームへ戻る", 'tweets#index'%>
  <%= link_to "マイページへ", 'users#show' %>
  <%= link_to "ログアウト", destroy_user_session_path, method: :delete %>
  <!-- <%= link_to "Back", :back %> -->
</div>
