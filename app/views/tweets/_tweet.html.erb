<div class="content_post" >
  <div class="more">
    <%= tweet.created_at %>
  </div>
  <%= simple_format(tweet.text) %>
  <span class="name">
      <a href="">
        <span>投稿者</span><%= tweet.user.email %>
      </a>
     </span>
</div>
